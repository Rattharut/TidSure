// postcss.config.js
// -----------------------------------------------------------------------------
// PostCSS คือขั้นตอนที่ Vite ใช้ประมวลผลไฟล์ CSS ก่อนส่งให้เบราว์เซอร์
//   - tailwindcss : แปลงคำสั่ง @tailwind / class ต่าง ๆ ให้กลายเป็น CSS จริง
//   - autoprefixer: เติม vendor prefix (เช่น -webkit-) ให้อัตโนมัติ รองรับหลายเบราว์เซอร์
// ไฟล์นี้ตั้งครั้งเดียวแล้วแทบไม่ต้องแตะอีก
// -----------------------------------------------------------------------------
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
